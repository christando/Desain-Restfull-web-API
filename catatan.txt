Untuk Run program API menggunakan command : dotnet watch run

commit to GitHub:

git status >> cek status file, branch dan perubahan di lokal
git add . >> Menandai Perubahan
git status >> cek status file, branch dan perubahan di lokal
git commit -m "Message" >> Menyimpan di Lokal
git pull >> Mengambil perubahan dari server git ke lokal
git push -u origin main >> menyimpan perubahan dari lokal ke server
git branch >> mengetahui semua branch yang ada

git checkout hashcode